{
  "name": "@shareplan/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "shareplan": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/cli.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@shareplan/core": "*",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.17.0"
  }
}
